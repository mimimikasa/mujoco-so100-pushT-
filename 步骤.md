#创建环境
```conda env create -f environment.yml```
#查看自己创建的环境
```conda env list```
#激活环境
```conda activate lerobot```
启动代码 采集数据
```cd /home/mikasa/pushT-so100/src
python3 env_human_ee.py
#按B开始录制采集数据```
也可以直接使用作者下好的数据集
```# 启动训练
cd /home/mikasa/pushT-so100/script
python3 train.py --data-path qian1dqs/so100-pusht```
查看训练模型推理效果
```# 查看自己训练好模型的训练效果
python3 infer.py   --ckpt_path /home/mikasa/pushT-so100/src/outputs/pusht_diffusion/checkpoints_2026-06-02_23:11/step_3000   --dataset_id qian1dqs/so100-pusht   --env_path "/home/mikasa/pushT-so100/mujoco_menagerie/human_env.xml"```
# 查看提供的推理效果
```python3 infer.py   --ckpt_path qian1dqs/so100-pusht-diffusion   --dataset_id qian1dqs/so100-pusht   --env_path "/home/mikasa/pushT-so100/mujoco_menagerie/human_env.xml"```
